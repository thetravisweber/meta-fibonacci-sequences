#include "SetUtils.h"
#include "HofUtils.h"

int main() 
{
    SetType baseSequence;

    initializeSet(&baseSequence);

    baseSequence

    for (int i = 0; i < 20; i++)
    {
        addToSequence(&baseSequence);
    }

    displaySet(baseSequence);

    return 1;
}